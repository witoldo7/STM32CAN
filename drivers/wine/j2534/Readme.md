Driver tested with Linux and Windows 10

# Install Linux userspace
Open terminall and follow:
```sh
mkdir build
cd build
cmake ../
make install
```
# Install Windows
Open project in Visual Studio as admin, comppile, install from menu.
